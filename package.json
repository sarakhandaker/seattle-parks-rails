{
  "name": "backend_parks",
  "version": "1.0.0",
  "description": "Seattle Parks Backend",
  "main": "index.js",
  "repository": {
    "url": "https://github.com/sarakhandaker/seattle-parks-rails",
    "type": "git"
  },
  "author": "y",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "cd client && yarn install && yarn build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "yarn build && yarn deploy && echo 'Client built!'"
  },
  "engines": {
    "yarn": "1.22.4",
    "npm": "6.14.5",
    "node": "v14.2.0"
  },
}
